# API

## mio.Resource

{{#each (classes)~}}
  {{>index~}}
{{/each~}}

## Events

{{#each (events)~}}
  {{>index~}}
{{/each}}

{{#each (classes)~}}
  {{>head}}
  {{>body}}
  {{>members}}
{{/each}}

{{#each (events)~}}
  {{>event}}
{{/each}}

{{#each (typedefs)~}}
  {{>typedef}}
{{/each}}
